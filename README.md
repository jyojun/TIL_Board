# TIL_Board

install packages
```
npm i express mongoose ejs slugify dompurify jsdom
```

```
npm i -g nodemon
```

package.json nodemon 설정
```json
"scripts": {
    "devStart": "nodemon server.js"
  }
```

실행
```
npm run devStart
```